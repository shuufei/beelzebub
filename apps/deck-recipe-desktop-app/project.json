{
  "name": "deck-recipe-desktop-app",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/deck-recipe-desktop-app",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nrwl/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "root": "apps/deck-recipe-desktop-app",
        "outputPath": "dist/apps/deck-recipe-desktop-app"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/deck-recipe-desktop-app"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nrwl/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "deck-recipe-desktop-app:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "deck-recipe-desktop-app:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "deck-recipe-desktop-app:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nrwl/next:export",
      "options": {
        "buildTarget": "deck-recipe-desktop-app:build:production"
      }
    },
    "test": {
      "executor": "@nrwl/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/deck-recipe-desktop-app/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "lint": {
      "executor": "@nrwl/linter:eslint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": [
          "apps/deck-recipe-desktop-app/**/*.{ts,tsx,js,jsx}"
        ]
      }
    }
  },
  "tags": []
}
